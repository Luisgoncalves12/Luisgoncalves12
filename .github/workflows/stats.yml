name: Update Stats

on:
  schedule:
    - cron: "0 6 * * *" # roda todo dia às 6h da manhã
  workflow_dispatch:

jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Atualizar estatísticas
        uses: anuraghazra/github-readme-stats@master
